<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yeoro.domain.user.model.mapper.UserMapper">

	<resultMap id="userResultMap" type="com.yeoro.domain.user.model.dto.UserDto">
		<result column="id" property="id"/>
		<result column="password" property="password"/>
		<result column="nickname" property="nickname"/>
		<result column="picture_url" property="picture_url"/>
		<result column="provider_type" property="provider_type"/>
		<result column="refresh_token" property="refresh_token"/>
		<result column="created_at" property="created_at"/>
		<result column="updated_at" property="updated_at"/>
	</resultMap>

	<select id="login" parameterType="com.yeoro.domain.user.model.dto.UserDto" resultMap="userResultMap">
		SELECT *
		FROM user
		WHERE id=#{id} and password=#{password}
	</select>

	<select id="userInfo" parameterType="string" resultMap="userResultMap">
		SELECT *
		FROM user
		WHERE id=#{id}
	</select>

	<insert id="saveRefreshToken" parameterType="map">
		INSERT INTO refresh_token (userid, token)
		VALUES (#{userid}, #{token})
	</insert>

	<select id="getRefreshToken" parameterType="string" resultType="string">
		SELECT token
		FROM refresh_token
		WHERE userid=#{userid}
	</select>

	<delete id="deleteRefreshToken" parameterType="map">
		DELETE FROM refresh_token
		WHERE userid=#{userid} AND token=#{token}
	</delete>

</mapper>
